<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Admin Chapter Management"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"/>

    <EditText
        android:id="@+id/chapterNumberInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Chapter Number"
        android:inputType="number"
        android:layout_below="@id/title"
        android:layout_marginTop="16dp"/>

    <EditText
        android:id="@+id/descriptionInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Description"
        android:layout_below="@id/chapterNumberInput"
        android:layout_marginTop="16dp"/>

    <EditText
        android:id="@+id/titleIdInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Title ID"
        android:layout_below="@id/descriptionInput"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/addChapterButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Chapter"
        android:layout_below="@id/titleIdInput"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/updateChapterButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Update Chapter"
        android:layout_below="@id/addChapterButton"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/deleteChapterButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Delete Chapter"
        android:layout_below="@id/updateChapterButton"
        android:layout_marginTop="16dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/deleteChapterButton"
        android:layout_marginTop="16dp"/>
</RelativeLayout>
